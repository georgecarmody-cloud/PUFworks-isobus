{
  "name": "pufworks-isobus-bench-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Bench UI for the PUFworks ISOBUS engine (heartbeat host + decoder)",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "electron:dev": "concurrently -k \"vite\" \"wait-on tcp:5173 && electron .\"",
    "electron": "electron ."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.0.0",
    "electron": "^31.0.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.0",
    "wait-on": "^8.0.0"
  }
}
